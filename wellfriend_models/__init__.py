"""Reproducible, artifact-first model research utilities for Wellfriend.

The package contains no production inference runtime, model weights, or dataset
payloads. It emits validated artifacts that the Rust perception runtime can consume.
"""

__version__ = "0.1.0"
