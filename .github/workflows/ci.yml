name: CI
on: [push, pull_request]
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python tools/format_check.py
      - run: python tools/lint.py
      - run: python -m unittest discover -s tests -v
      - run: python export/validate_artifact.py --allow-placeholder registry/document-detector

