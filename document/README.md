# Document models

`detector`, `corners`, `quality`, `restoration`, `cleanup`, and `ocr` hold independent research tracks that export through the shared artifact contract. MP5 supplies CPU reference interfaces and synthetic smoke evidence only; no directory includes trained weights or a production quality claim.
