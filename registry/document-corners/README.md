# Document corners registry family

The checked-in MP5 artifact is a no-weights placeholder for `document_corner_regression`. It cannot be used for capture geometry until a released artifact has provenance and validated weights.
