# Versioning

`0.1.0-alpha.1` is an unstable governance API. `placeholder` has no weights; `experimental` is synthetic/no-weight; `candidate` and `mobile_candidate` require evidence; `production_ready` is future-only and blocked without complete provenance and runtime validation.
