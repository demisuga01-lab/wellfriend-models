# Ecosystem smoke checklist

Run the perception Rust CI commands, this repository’s Python CI commands plus `python tools/check_ecosystem_compatibility.py`, and scanner build/tests/contracts. Compare the compatibility manifest before claiming cross-repository support.
