# Getting started

Create a Python 3.11 environment, run `python -m pip install -e ".[dev]"`, then `pytest`. Models and datasets are governed here; algorithms live in `wellfriend-perception` and product UI in `wellfriend-scan`.
