# ADR-0010: Quantization and pruning report policy

Quantization and pruning are reportable evidence, not metadata-based production promotion. Base CI validates contracts; optional runtimes validate real graphs only when explicitly installed.
