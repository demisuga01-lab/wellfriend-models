# ADR-0007: No production claims from placeholder models

Placeholder registry artifacts deliberately have no weights and `production_ready: false`. Tests may validate their structure only with an explicit placeholder permission.
