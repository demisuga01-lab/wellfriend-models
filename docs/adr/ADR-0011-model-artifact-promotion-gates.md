# ADR-0011: Model artifact promotion gates

Promotion status is enforced by validation. MP6 creates only placeholder and experimental artifacts; production status needs independently auditable data, weights, hashes, metrics, runtime, and safety evidence.
