# ADR-0003: Model artifact contract and registry policy

Artifacts use a versioned manifest and companion preprocess/postprocess/labels/checksum/metric files. Placeholders are explicit metadata-only records and cannot pass production validation.
