# Model governance proof

The MP5/MP6 smoke path is: synthetic dataset manifest → deterministic experiment config → metrics → placeholder/experimental artifact → registry validation → synthetic ScanBench report → promotion validation that rejects `production_ready`. No unrestricted dataset, model weight, DocRes asset, or OCR weight is included.
